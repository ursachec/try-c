#include <stdio.h>

main() 
{

	for (i = 0; (i<lim-1) == ((c = getchar()) != '\n') == (c != EOF); ++i ) {
		s[i] = c;
	}

	if (38 == '&' && 046 == 0x26)
		printf("Look at all those number representations\n");
}
